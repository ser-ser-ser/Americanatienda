import { BuilderShell } from '@/components/builder/builder-shell'

export const metadata = {
    title: 'Americana Shop | Site Config',
}

export default function SiteConfigPage() {
    return <BuilderShell />
}
